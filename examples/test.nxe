// Pointer to the character output register
var chrout: ptr[u8] = 0x4000;

// Print a null-terminated string
// fn print(p: ptr[u8]) {
//     while (@p) {
//         @chrout = @p;
//         p = p + 1;
//     }
// }

fn putchar(c: u8) {
    @chrout = c;
}

const var msg: str = "Hello World";

__interrupt(reset) fn main() {
    // print(msg);
    while (true) {
        putchar('H'); // 'H');
    }
}